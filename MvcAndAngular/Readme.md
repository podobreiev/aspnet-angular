Create ASP.NET MVC app.
In the app create Angular app:
ng new angular-app
configure .angular.cli.json to output files to needed dir
configure build in VS, create pre build script
cd ../angular-app &
ng build &
cd ../bin